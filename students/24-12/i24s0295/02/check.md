 * Функция count не решает поставленную задачу, если параметров, например, два.
 * Функция sum работала бы и без `if`
 * `isArrowFunction` - целая куча условий, и оно работает, но можно было гораздо проще.
